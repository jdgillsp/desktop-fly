DesktopFly
.DS_Store
*.log
nohup.out
/*.png

# Rust port (target-* are the side build dirs used while a copy of the app
# holds target/release/desktopfly.exe - see HABITAT_PLAN.md sec 4)
rust/target/
rust/target-*/

# C. elegans connectome: NOT shipped. PORT_PLAN.md sec 8 flags its
# redistribution terms as unverified. Generate it locally with
# etl_celegans.py and check the licence before committing anything here.
data/c_elegans/
